const num1 = 5
const num2 = 10

function addValues() {
  console.log(`the sum is : ${num1 + num2}`)
}

addValues()

//when we are setting up everything
//f we have a function inside that module
//that we invoke,that code will run
//even though we don't assign it to a variable in 3-module.js